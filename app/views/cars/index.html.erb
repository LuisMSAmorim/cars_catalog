<p id="notice"><%= notice %></p>

<h1 class="tittle">Carros disponíveis</h1>

<table class="table">
  <thead>
    <tr>
      <th>Modelo</th>
      <th>Marca</th>
      <th>Color</th>
      <th>Year</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.model %></td>
        <td><%= car.brand %></td>
        <td><%= car.color %></td>
        <td><%= car.year %></td>
        <td><%= car.value %></td>
        <td><%= link_to 'Show', car %></td>
        <td><%= link_to 'Edit', edit_car_path(car) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Car', new_car_path %>
